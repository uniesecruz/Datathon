import React from 'react';
import ReactDOM from 'react-dom/client';

const App = () => (
  <div>
    <h1>Dashboard LLM - RAG Recrutamento</h1>
    {/* Adapte para importar e usar seus componentes ou lógica */}
  </div>
);

const root = ReactDOM.createRoot(document.getElementById('root'));
root.render(<App />);
